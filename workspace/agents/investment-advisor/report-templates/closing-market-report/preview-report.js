const reportData = {"meta": {"pageTitle": "理财助手｜A股收盘报告｜2026-03-11", "badge": "Closing Report", "reportId": "CLOSE-2026-03-11", "heading": "A股收盘报告｜成长占优，新能源设备链成为最清晰主线", "generatedAt": "2026-03-11 15:05 Asia/Shanghai", "marketScope": "A股为主｜兼顾港股、美股、宏观映射", "styleBias": "高端商业简报 × 科技仪表盘 × 技术面增强", "sourceLabel": "数据来源：腾讯行情、东方财富板块数据、公开财经资讯", "versionLabel": "模板版本：closing-market-report v0.2"}, "summary": "今天市场整体呈现温和走强、成长领先、资源回落的结构，指数层面并不亢奋，但新能源设备链与部分化工方向形成了比较清晰的当日主线。", "conclusion": {"text": "市场资金正在从资源金属一侧切出，转向新能源设备链和部分景气制造方向。", "tags": ["成长领先", "新能源主线", "资源回落"]}, "summaryCards": [{"title": "指数结构偏强", "text": "上证涨 0.25%，深成指涨 0.78%，创业板涨 1.31%，明显是成长风格强于权重风格的一天。"}, {"title": "主线高度集中", "text": "逆变器、储能、风电等方向同步走强，说明今天的市场并非无序轮动，而是有相对明确的进攻核心。"}, {"title": "资源方向承压", "text": "钨、稀土、钼、白银、小金属集体回调，表明前期资源方向面临一定获利回吐和风格切换压力。"}], "metrics": [{"name": "上证指数", "value": "4133.43", "change": "+0.25%", "changeClass": "positive", "note": "指数温和走强，但不是全面普涨的进攻日。"}, {"name": "深证成指", "value": "14465.41", "change": "+0.78%", "changeClass": "positive", "note": "弹性明显好于沪市，更能体现成长偏好。"}, {"name": "创业板指", "value": "3349.53", "change": "+1.31%", "changeClass": "positive", "note": "今天最强指数，说明资金更愿意配置高弹性方向。"}, {"name": "沪深成交额", "value": "2.5万亿+", "change": "较上日放量约1022亿", "changeClass": "positive", "note": "放量说明这不是纯缩量脉冲，具备一定增量特征。"}, {"name": "市场风格", "value": "成长领先", "change": "资源回落", "changeClass": "negative", "note": "市场出现明显的高低切换与方向切换。"}], "technicalCharts": [{"name": "上证指数", "badge": "震荡偏强", "structure": "震荡偏强", "maStatus": "站上 MA5 / MA10，接近 MA20 上方延续", "macdStatus": "红柱延续，DIF 位于 DEA 上方", "volumeStatus": "放量修复，量能配合尚可", "conclusion": "上证今天更像稳步修复而非单边突破，技术面偏强但趋势斜率并不激进，更适合作为市场风险偏好的稳定锚。", "candles": [{"open": 4062, "high": 4078, "low": 4048, "close": 4071}, {"open": 4070, "high": 4084, "low": 4056, "close": 4061}, {"open": 4060, "high": 4098, "low": 4058, "close": 4089}, {"open": 4090, "high": 4106, "low": 4075, "close": 4080}, {"open": 4082, "high": 4115, "low": 4079, "close": 4108}, {"open": 4109, "high": 4122, "low": 4091, "close": 4116}, {"open": 4115, "high": 4138, "low": 4106, "close": 4133}], "ma5": [4069, 4075, 4082, 4089, 4103, 4110, 4125], "ma10": [4058, 4062, 4068, 4072, 4080, 4090, 4101], "ma20": [4046, 4050, 4055, 4060, 4068, 4076, 4085], "volume": [1.42, 1.36, 1.55, 1.48, 1.63, 1.7, 1.82], "macd": {"dif": [0.08, 0.11, 0.16, 0.18, 0.24, 0.29, 0.34], "dea": [0.02, 0.05, 0.09, 0.12, 0.16, 0.2, 0.25], "hist": [0.12, 0.14, 0.18, 0.17, 0.21, 0.24, 0.27]}}, {"name": "深证成指", "badge": "修复增强", "structure": "修复增强", "maStatus": "MA5 明显上穿 MA10，MA20 开始走平", "macdStatus": "金叉后红柱继续放大", "volumeStatus": "量价同步改善，弹性更优", "conclusion": "深成指的技术形态比上证更积极，说明资金在更高弹性资产中做了更明确的方向表达，属于今天技术面最顺的一类指数。", "candles": [{"open": 14010, "high": 14086, "low": 13982, "close": 14058}, {"open": 14060, "high": 14122, "low": 14012, "close": 14028}, {"open": 14032, "high": 14188, "low": 14020, "close": 14152}, {"open": 14154, "high": 14236, "low": 14110, "close": 14170}, {"open": 14175, "high": 14332, "low": 14148, "close": 14298}, {"open": 14300, "high": 14426, "low": 14280, "close": 14396}, {"open": 14400, "high": 14502, "low": 14376, "close": 14465}], "ma5": [14054, 14088, 14141, 14181, 14261, 14323, 14396], "ma10": [13992, 14018, 14052, 14096, 14140, 14198, 14265], "ma20": [13930, 13950, 13972, 14001, 14036, 14074, 14120], "volume": [1.55, 1.48, 1.72, 1.66, 1.89, 1.96, 2.05], "macd": {"dif": [0.09, 0.15, 0.23, 0.31, 0.42, 0.52, 0.61], "dea": [0.03, 0.07, 0.12, 0.18, 0.27, 0.36, 0.45], "hist": [0.12, 0.16, 0.22, 0.26, 0.3, 0.32, 0.32]}}, {"name": "创业板指", "badge": "强势修复", "structure": "强势修复", "maStatus": "多条短中期均线呈多头排列", "macdStatus": "金叉后红柱扩张最明显", "volumeStatus": "量能跟随放大，趋势确认度较高", "conclusion": "创业板是今天技术面最具辨识度的指数，均线、量能、MACD 三者共振，说明成长风格获得了最强确认。", "candles": [{"open": 3230, "high": 3252, "low": 3218, "close": 3248}, {"open": 3246, "high": 3261, "low": 3228, "close": 3236}, {"open": 3238, "high": 3284, "low": 3230, "close": 3270}, {"open": 3272, "high": 3298, "low": 3260, "close": 3286}, {"open": 3288, "high": 3314, "low": 3278, "close": 3306}, {"open": 3308, "high": 3338, "low": 3296, "close": 3326}, {"open": 3328, "high": 3361, "low": 3318, "close": 3349}], "ma5": [3244, 3250, 3261, 3276, 3292, 3310, 3327], "ma10": [3228, 3235, 3245, 3256, 3270, 3286, 3302], "ma20": [3208, 3214, 3220, 3228, 3238, 3250, 3264], "volume": [1.18, 1.14, 1.32, 1.39, 1.56, 1.64, 1.82], "macd": {"dif": [0.11, 0.16, 0.28, 0.39, 0.52, 0.66, 0.79], "dea": [0.02, 0.06, 0.13, 0.21, 0.31, 0.42, 0.54], "hist": [0.16, 0.2, 0.28, 0.36, 0.42, 0.48, 0.5]}}], "indexChart": [{"label": "上证", "value": 0.25, "display": "+0.25%"}, {"label": "深成指", "value": 0.78, "display": "+0.78%"}, {"label": "创业板", "value": 1.31, "display": "+1.31%"}], "leadersChart": [{"label": "逆变器", "value": 8.3, "display": "+8.30%"}, {"label": "储能", "value": 7.6, "display": "+7.60%"}, {"label": "钛白粉", "value": 5.58, "display": "+5.58%"}, {"label": "纯碱", "value": 5.13, "display": "+5.13%"}, {"label": "煤化工", "value": 4.99, "display": "+4.99%"}], "laggardsChart": [{"label": "钨", "value": -4.27, "display": "-4.27%"}, {"label": "稀土", "value": -3.75, "display": "-3.75%"}, {"label": "钼", "value": -3.38, "display": "-3.38%"}, {"label": "白银", "value": -2.67, "display": "-2.67%"}, {"label": "小金属", "value": -2.21, "display": "-2.21%"}], "structureAnalysis": ["今天最重要的不是指数都收红，而是指数背后的结构非常明确：创业板显著强于上证，说明市场正在把注意力重新放回高弹性成长方向，而不是简单围绕权重和资源做防守型配置。", "从板块层面看，逆变器、储能、风电等新能源设备链成为最清晰的进攻线索，说明当前市场愿意为产业弹性、景气修复和中期想象空间支付更高溢价。", "与此同时，钨、稀土、钼、白银等资源方向集体回调，反映出前期拥挤交易出现松动。也就是说，今天的核心不是“所有板块一起强”，而是资金正在重新做方向选择。"], "nextWatch": ["继续跟踪逆变器、储能、风电等方向明天是否还能获得持续性承接。", "观察化工景气链今天的强势，是短期事件驱动还是阶段性风格回补。", "关注创业板能否继续强于上证，这是验证成长风格是否延续的核心信号。"], "risks": ["如果明天新能源链明显冲高回落，今天的强势可能更像集中宣泄而非持续主线。", "如果资源方向继续大幅回调，同时成长方向又接不住，市场可能重新回到高频轮动。", "如果成交额不能继续维持在高位，今天的结构性进攻强度可能会被削弱。"], "detailRows": [{"dimension": "上证指数", "value": "4133.43", "change": "+0.25%", "interpretation": "指数偏稳，不是全面共振式大涨。"}, {"dimension": "深证成指", "value": "14465.41", "change": "+0.78%", "interpretation": "代表弹性资产表现更优。"}, {"dimension": "创业板指", "value": "3349.53", "change": "+1.31%", "interpretation": "成长风格今天最具辨识度。"}, {"dimension": "恒生指数", "value": "25923.02", "change": "-0.14%", "interpretation": "港股并未同步强势，A股更偏内部结构驱动。"}, {"dimension": "隔夜道指", "value": "47706.51", "change": "-0.07%", "interpretation": "外围环境中性偏稳，没有形成明显拖累。"}]};

function createEl(tag, className, text) {
  const el = document.createElement(tag);
  if (className) el.className = className;
  if (text !== undefined && text !== null) el.textContent = text;
  return el;
}

function mountTags() {
  const box = document.getElementById('highlight-tags');
  (reportData.conclusion.tags || []).forEach((tag) => {
    box.appendChild(createEl('span', 'highlight-tag', tag));
  });
}

function mountSummaries() {
  const grid = document.getElementById('summary-grid');
  (reportData.summaryCards || []).forEach((item) => {
    const card = createEl('article', 'summary-card');
    card.appendChild(createEl('h3', '', item.title));
    card.appendChild(createEl('p', '', item.text));
    grid.appendChild(card);
  });
}

function mountMetrics() {
  const grid = document.getElementById('metric-grid');
  (reportData.metrics || []).forEach((item) => {
    const card = createEl('article', 'metric-card');
    card.appendChild(createEl('div', 'metric-name', item.name));
    card.appendChild(createEl('div', 'metric-value', item.value));
    const change = createEl('div', `metric-change ${item.changeClass || ''}`, item.change);
    card.appendChild(change);
    card.appendChild(createEl('div', 'metric-note', item.note));
    grid.appendChild(card);
  });
}

function mountBars(targetId, items, negative = false) {
  const root = document.getElementById(targetId);
  const max = Math.max(...items.map((x) => Math.abs(x.value)), 1);

  items.forEach((item) => {
    const row = createEl('div', 'bar-item');
    row.appendChild(createEl('div', 'bar-label', item.label));

    const track = createEl('div', 'bar-track');
    const fill = createEl('div', `bar-fill ${negative ? 'negative' : ''}`);
    fill.style.width = `${Math.max((Math.abs(item.value) / max) * 100, 6)}%`;
    track.appendChild(fill);

    row.appendChild(track);
    row.appendChild(createEl('div', `bar-value ${negative ? 'negative' : 'positive'}`, item.display));
    root.appendChild(row);
  });
}

function mountStructure() {
  const box = document.getElementById('structure-analysis');
  (reportData.structureAnalysis || []).forEach((text) => {
    box.appendChild(createEl('p', '', text));
  });
}

function mountList(targetId, items) {
  const root = document.getElementById(targetId);
  items.forEach((item) => {
    root.appendChild(createEl('li', '', item));
  });
}

function mountTable() {
  const body = document.getElementById('detail-table');
  (reportData.detailRows || []).forEach((row) => {
    const tr = document.createElement('tr');
    [row.dimension, row.value, row.change, row.interpretation].forEach((cell) => {
      const td = document.createElement('td');
      td.textContent = cell;
      tr.appendChild(td);
    });
    body.appendChild(tr);
  });
}

function svgEl(tag, attrs = {}) {
  const el = document.createElementNS('http://www.w3.org/2000/svg', tag);
  Object.entries(attrs).forEach(([k, v]) => el.setAttribute(k, v));
  return el;
}

function linePath(points) {
  if (!points.length) return '';
  return points.map((p, i) => `${i === 0 ? 'M' : 'L'} ${p[0]} ${p[1]}`).join(' ');
}

function mountTechnicalCharts() {
  const root = document.getElementById('technical-grid');
  (reportData.technicalCharts || []).forEach((chart) => {
    const card = createEl('article', 'tech-card');
    const head = createEl('div', 'tech-card-head');
    head.appendChild(createEl('h3', '', chart.name));
    head.appendChild(createEl('span', 'tech-badge', chart.badge));
    card.appendChild(head);

    const svg = buildTechSvg(chart);
    svg.classList.add('tech-svg');
    card.appendChild(svg);

    const metrics = createEl('div', 'tech-metrics');
    [
      ['结构状态', chart.structure],
      ['均线状态', chart.maStatus],
      ['MACD 状态', chart.macdStatus],
      ['量价关系', chart.volumeStatus]
    ].forEach(([label, value]) => {
      const item = createEl('div', 'tech-metric');
      item.appendChild(createEl('span', 'tech-metric-label', label));
      item.appendChild(createEl('span', 'tech-metric-value', value));
      metrics.appendChild(item);
    });
    card.appendChild(metrics);
    card.appendChild(createEl('div', 'tech-conclusion', chart.conclusion));
    root.appendChild(card);
  });
}

function buildTechSvg(chart) {
  const width = 360;
  const height = 230;
  const svg = svgEl('svg', { viewBox: `0 0 ${width} ${height}`, role: 'img', 'aria-label': `${chart.name}技术图` });

  const bg = svgEl('rect', { x: 0, y: 0, width, height, rx: 16, fill: 'transparent' });
  svg.appendChild(bg);

  const plot = { x: 18, y: 18, w: width - 36, h: 110 };
  const vol = { x: 18, y: 138, w: width - 36, h: 28 };
  const macd = { x: 18, y: 175, w: width - 36, h: 36 };

  for (let i = 0; i <= 4; i++) {
    const y = plot.y + (plot.h / 4) * i;
    svg.appendChild(svgEl('line', { x1: plot.x, y1: y, x2: plot.x + plot.w, y2: y, stroke: 'rgba(196,132,83,0.14)', 'stroke-width': 1 }));
  }

  const prices = chart.candles.flatMap(c => [c.high, c.low, c.open, c.close]);
  const pMin = Math.min(...prices);
  const pMax = Math.max(...prices);
  const priceY = (v) => plot.y + (1 - (v - pMin) / ((pMax - pMin) || 1)) * plot.h;

  const step = plot.w / chart.candles.length;
  const bodyW = Math.max(step * 0.52, 8);

  chart.candles.forEach((c, idx) => {
    const cx = plot.x + step * idx + step / 2;
    const color = c.close >= c.open ? '#12966b' : '#d94f3d';
    svg.appendChild(svgEl('line', { x1: cx, y1: priceY(c.high), x2: cx, y2: priceY(c.low), stroke: color, 'stroke-width': 1.6 }));
    const top = Math.min(priceY(c.open), priceY(c.close));
    const h = Math.max(Math.abs(priceY(c.open) - priceY(c.close)), 2.5);
    svg.appendChild(svgEl('rect', { x: cx - bodyW / 2, y: top, width: bodyW, height: h, rx: 2, fill: color, opacity: 0.9 }));
  });

  ['ma5', 'ma10', 'ma20'].forEach((key, idx) => {
    const colors = ['#f26b1d', '#f7b267', '#6c8bd8'];
    const vals = chart[key] || [];
    const pts = vals.map((v, i) => [plot.x + step * i + step / 2, priceY(v)]);
    svg.appendChild(svgEl('path', {
      d: linePath(pts),
      fill: 'none',
      stroke: colors[idx],
      'stroke-width': 2,
      'stroke-linecap': 'round',
      'stroke-linejoin': 'round'
    }));
  });

  const volMax = Math.max(...chart.volume, 1);
  chart.volume.forEach((v, idx) => {
    const x = vol.x + step * idx + step * 0.18;
    const h = (v / volMax) * vol.h;
    const candle = chart.candles[idx];
    const color = candle.close >= candle.open ? 'rgba(18,150,107,0.68)' : 'rgba(217,79,61,0.68)';
    svg.appendChild(svgEl('rect', { x, y: vol.y + vol.h - h, width: step * 0.64, height: h, rx: 2, fill: color }));
  });

  const macdAbs = Math.max(...chart.macd.hist.map(v => Math.abs(v)), 0.01);
  const macdZero = macd.y + macd.h / 2;
  svg.appendChild(svgEl('line', { x1: macd.x, y1: macdZero, x2: macd.x + macd.w, y2: macdZero, stroke: 'rgba(196,132,83,0.22)', 'stroke-width': 1 }));
  chart.macd.hist.forEach((v, idx) => {
    const h = Math.abs(v) / macdAbs * (macd.h / 2 - 2);
    const x = macd.x + step * idx + step * 0.22;
    const y = v >= 0 ? macdZero - h : macdZero;
    svg.appendChild(svgEl('rect', { x, y, width: step * 0.56, height: h, rx: 2, fill: v >= 0 ? 'rgba(18,150,107,0.75)' : 'rgba(217,79,61,0.75)' }));
  });

  const macdLineY = (v) => macd.y + (1 - (v + macdAbs) / (2 * macdAbs || 1)) * macd.h;
  [['dif', '#f26b1d'], ['dea', '#6c8bd8']].forEach(([key, color]) => {
    const pts = chart.macd[key].map((v, i) => [macd.x + step * i + step / 2, macdLineY(v)]);
    svg.appendChild(svgEl('path', {
      d: linePath(pts), fill: 'none', stroke: color, 'stroke-width': 1.8,
      'stroke-linecap': 'round', 'stroke-linejoin': 'round'
    }));
  });

  const labels = [
    ['K线 / MA', plot.x, plot.y - 4],
    ['VOL', vol.x, vol.y - 4],
    ['MACD', macd.x, macd.y - 4]
  ];
  labels.forEach(([text, x, y]) => {
    const t = svgEl('text', { x, y, fill: '#7a6556', 'font-size': 10, 'font-weight': 700 });
    t.textContent = text;
    svg.appendChild(t);
  });

  return svg;
}

function init() {
  mountTags();
  mountSummaries();
  mountMetrics();
  mountTechnicalCharts();
  mountBars('index-chart', reportData.indexChart || []);
  mountBars('leaders-chart', reportData.leadersChart || []);
  mountBars('laggards-chart', reportData.laggardsChart || [], true);
  mountStructure();
  mountList('next-watch-list', reportData.nextWatch || []);
  mountList('risk-list', reportData.risks || []);
  mountTable();
}

init();